# 2026-09-05 — UX fragile-surface pass (~8.5 target)

Branch: `feat/ux-8-5-fragile-surfaces`
Status: merged via PR #12 on 2026-09-05.

| Gap | Change |
|---|---|
| Commitment disclosure leak | Human bounds on primary cards; stack names nested under "Technical details" |
| Empty/error craft | `noFitLine` on Listening empty; `offlineLine` added; `/memory` delete uses `memoryDeletedLine` |
| Marketplace gravity | Vitest guard on `ArrivalScreen` + home `page.tsx` forbidding browse/catalog patterns |
| Operator ritual | Briefing is the serif letter hero ("This morning"); retreat table demoted under a tag |
| Wow vs clarity | `CommitmentArc` always shows a meaning line (deposit confirm), not only motion |
| Secondary tone | `/memory` and `/attest` intros rewritten in Mira/operator letter register |

Standing: first-session observation still needed before calling arrival 9/10.
Open follow-up: the PR test-plan manual UI checks (commit disclosure,
`/operator` briefing hero, `/memory` tone, listening empty state) were
deferred at merge; revisit on next live pass.

## Follow-up — first-session arrival (same PR)

Live/local observation: without bootstrap, first paint was a centered "Mira"
with no input (client fetch). Stagger also started at opacity 0.

Fixes:
- Home always passes `episodeBootstrap` (null episode ok) so SSR renders the ask
- `StaggerReveal eager` on arrival intention/returning
- Autofocus + `data-testid="arrival-intention-input"`
- Privacy note is a real `/memory` link
- Create errors use `providerFailureLine`

Verified: `GET /` HTML contains "make space for" + textarea on first byte stream.

## Follow-up — returning / commit ceremony

- Securing surface: rail steps moved under collapsed "Progress details";
  chips are ready/held/proof (not escrow); "Preparing your account" no longer
  a user-facing phase label.
- BookedLanding "How this is secured" matches grant human copy + nested
  technical details.
- Returning visitor path already one primary Continue CTA; beckon still off on `/`.
